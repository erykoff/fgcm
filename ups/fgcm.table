setupRequired(esutil)
setupRequired(numpy)
setupRequired(scipy)
setupRequired(matplotlib)
setupRequired(healpy)
setupRequired(pyyaml)

envAppend(PYTHONPATH, ${PRODUCT_DIR}/)
envAppend(PATH, ${PRODUCT_DIR}/scripts)

